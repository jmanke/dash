{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "www/**"]
    },
    "build:components": {
      "dependsOn": ["^build:utils"],
      "outputs": ["dist/**", "www/**"]
    },
    "build:hellodash-components": {
      "dependsOn": ["^build:components", "^build:hellodash-models"],
      "outputs": ["dist/**", "www/**"]
    },
    "build:dev": {
      "dependsOn": ["^build:dev"],
      "outputs": ["dist/**", "www/**"]
    },
    "build:hellodash-models": {
      "outputs": ["dist/**"]
    },
    "build:utils": {
      "outputs": ["dist/**", "www/**"]
    },
    "start:hellodash": {
      "dependsOn": ["^build:hellodash-components"],
      "outputs": ["dist/**"]
    },
    "start:components": {
      "dependsOn": ["^build:utils"]
    },
    "start:components:forge": {}
  }
}
